import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#7562E0",
            dark: "#000036",
        },
        secondary: {
            main: "#282162"
        }
    }
})

export default theme