import { Box, Link, styled } from "@mui/material";

export const StyledResponsiveBox = styled(Box)`

    display: flex;
    align-items: center;
    gap: 20px;
`

export const StyledLink = styled(Link)`
    font-size: 1.25rem;
    color: white;
`